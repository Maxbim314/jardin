function enviar() {

  var username = [document.getElementById("nombre").value];
  var usersug = document.getElementById("sugerencia").value;
  var addComentario=document.getElementById("addComentario");
  //addComentario.innerHTML="@" + username + ": " + usersug;
  addComentario.innerHTML="@Max: Ajam, te engañé, por el momento no está disponible la sección de comentarios :)";
}
